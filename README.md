# Fruits & Vegetables Deep Learning Classifier

A deep learning image classification system that identifies **36 classes** of fruits and vegetables using fine-tuned convolutional neural networks. University deep learning project - 3-person team, each training a different backbone on the same dataset.

---

## Project Structure

```
Fruits_Vegetables_Classification_DL/
│
├── 00_preprocessing.ipynb      # Dataset exploration - run first
├── 01_mobilenet.ipynb          # MobileNetV2 fine-tuning
├── 02_resnet.ipynb             # ResNet50 fine-tuning
├── 03_efficientnet.ipynb       # EfficientNetB0 fine-tuning
├── 04_comparison.ipynb         # Final cross-model comparison
├── 05_vgg.ipynb                # VGG16 fine-tuning
│
├── class_names.json            # 36 class labels (generated by 01)
├── requirements.txt
└── README.md
```

---

## Dataset

**Source:** [Fruit and Vegetable Image Recognition - Kaggle](https://www.kaggle.com/datasets/kritikseth/fruit-and-vegetable-image-recognition)

```
train/
├── fruit/       → 8 classes  (apple, banana, grapes, kiwi, mango,
│                               pineapple, pomegranate, watermelon)
└── vegetable/   → 28 classes (beetroot, bell pepper, cabbage, capsicum,
                                carrot, cauliflower, chilli pepper, corn,
                                cucumber, eggplant, garlic, ginger, jalepeno,
                                lemon, lettuce, onion, orange, paprika, pear,
                                peas, potato, raddish, soy beans, spinach,
                                sweetcorn, sweetpotato, tomato, turnip)
test/            → same structure
validation/      → IGNORED (flat, inconsistent with train/test)
```

**Stats:** 3,115 train | 359 test | 36 classes | ~86 images/class (CV = 10.6%, well balanced)

**Validation split:** 20% stratified split of `train/`, `random_state=42`.

---

## Architecture Pattern

All models follow the same two-phase fine-tuning strategy:

```
Input Image (224×224×3)  →  [0, 1] float32
        ↓
  Model-specific preprocessing layer (built into model)
        ↓
  Pretrained Backbone (ImageNet weights)
        ↓
  Global Average Pooling
        ↓
  Dense Classification Head
        ↓
  Dense(36, softmax)
```

**Phase 1 - Feature Extraction:** base frozen, head only (Adam 1e-3, ≤10 epochs, EarlyStopping)  
**Phase 2 - Fine-tuning:** top layers unfrozen, low LR (Adam 1e-5, ≤20 epochs, EarlyStopping)

---

## Model Details

### MobileNetV2 - `01_mobilenet.ipynb`

| Item | Value |
|------|-------|
| Preprocessing | `Rescaling(×2, −1)` → [−1, 1] |
| Base | 154 layers, freeze first 100 |
| Head | GAP → BN → Dropout(0.3) → Dense(36, softmax) |
| Test accuracy | **93.04%** |
| Saved as | `mobilenet.keras` |

### ResNet50 - `02_resnet.ipynb`

| Item | Value |
|------|-------|
| Preprocessing | `Rescaling(×255)` → `resnet50.preprocess_input` (BGR, mean-subtracted) |
| Base | 175 layers, fine-tune from layer 140 |
| Head | GAP → BN → Dropout(0.3) → Dense(36, softmax) |
| Test accuracy | **97.21%** |
| Saved as | `resnet50_rawan.keras` |

### EfficientNetB0 - `03_efficientnet.ipynb`

| Item | Value |
|------|-------|
| Preprocessing | Built-in (accepts [0, 255] directly) |
| Base | 237 layers, fine-tune last 30 |
| Head | Dropout(0.2) → Dense(256, relu) → Dropout(0.2) → Dense(36, softmax) |
| Test accuracy | **95.82%** |
| Saved as | `efficientnet.keras` |

### VGG16 - `05_vgg.ipynb`

| Item | Value |
|------|-------|
| Preprocessing | `vgg16.preprocess_input` (BGR, mean-subtracted) via custom layer |
| Base | 19 layers, fine-tune block5 (layers 15+) |
| Head | GAP → Dense(256, relu) → BN → Dropout(0.4) → Dense(36, softmax) |
| Test accuracy | **96.66%** |
| Saved as | `vgg.keras` |

---

## Environment

**Python 3.10 required** - TF 2.21.0 has no wheels for Python 3.14+ (Windows default).

```powershell
py -3.10 -m pip install -r requirements.txt
py -3.10 -m jupyter notebook
```

Select the **Python 3.10** kernel in VS Code before running any notebook.

---

## Execution Order

```
1. 00_preprocessing.ipynb    ← run once, verify dataset, generates class_names.json
2. 01, 02, 03, 05            ← run independently in any order
3. 04_comparison.ipynb       ← run after all models are trained and saved
```

---

## Augmentation (training only, applied via tf.data)

- Random horizontal flip
- Random brightness ± 15%
- Random contrast 0.85–1.15×
- Random saturation 0.85–1.15×

---

## Evaluation

Each model notebook reports on the held-out `test/` set (never used during training):
- Overall accuracy
- Per-class precision, recall, F1-score (sklearn classification report)
- Confusion matrix heatmap

`04_comparison.ipynb` compares all models side-by-side on the same test set.

---

## Context

This is the deep learning continuation of an ML project from the previous semester which compared 6 classical classifiers (Decision Tree, Random Forest, XGBoost, KNN, SVM, ANN) on frozen MobileNetV2 features using a 2-stage hierarchical pipeline.

| | ML Project | DL Project |
|--|-----------|-----------|
| Approach | Frozen features + classical classifier | End-to-end fine-tuning |
| Pipeline | 2-stage (binary → multi-class) | Single-stage (36 classes directly) |
| Models | DT, RF, XGBoost, KNN, SVM, ANN | MobileNetV2, ResNet50, EfficientNetB0, VGG16 |
| Best accuracy | 98% (KNN) | 97.21% (ResNet50) |
